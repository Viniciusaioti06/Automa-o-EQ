(() => {const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const items=document.querySelectorAll('.reveal');if(!('IntersectionObserver' in window)||reduced){items.forEach(x=>x.classList.add('is-visible'))}else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.14});items.forEach((x,i)=>{x.style.transitionDelay=`${Math.min(i%4,3)*70}ms`;io.observe(x)})}const counters=document.querySelectorAll('.counter');const run=(el)=>{const end=+el.dataset.count,start=performance.now(),dur=1200;const frame=t=>{const p=Math.min((t-start)/dur,1);el.textContent=Math.floor(end*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(frame)};requestAnimationFrame(frame)};const cio=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){run(e.target);cio.unobserve(e.target)}}));counters.forEach(x=>cio.observe(x));if(!reduced&&matchMedia('(pointer:fine)').matches){const card=document.querySelector('.tilt-card');document.querySelector('.hero__visual')?.addEventListener('mousemove',e=>{const r=e.currentTarget.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`rotateY(${x*10}deg) rotateX(${-y*8}deg)`});document.querySelector('.hero__visual')?.addEventListener('mouseleave',()=>card.style.transform='rotateY(-5deg) rotateX(3deg)');const dot=document.querySelector('.cursor-dot'),ring=document.querySelector('.cursor-ring');addEventListener('mousemove',e=>{dot.style.left=ring.style.left=e.clientX+'px';dot.style.top=ring.style.top=e.clientY+'px'});document.querySelectorAll('a,button').forEach(el=>{el.addEventListener('mouseenter',()=>{ring.style.width='48px';ring.style.height='48px'});el.addEventListener('mouseleave',()=>{ring.style.width='34px';ring.style.height='34px'})})}})();